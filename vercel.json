{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install",
  "github": {
    "silent": true,
    "autoAlias": true
  },
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ./",
  "workspaceCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install",
  "framework": "nextjs"
}