{
  "name": "cipc-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "patch-package || true"
  },
  "devDependencies": {
    "rimraf": "^6.1.0",
    "turbo": "^1.9.3"
  }
}
